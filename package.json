{
  "name": "http-server",
  "version": "1.0.0",
  "description": "\"Build a simple HTTP Server using Node JS\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "Node index",
    "start": "nodemon index.js"
  },
  "author": "Abdullah",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
